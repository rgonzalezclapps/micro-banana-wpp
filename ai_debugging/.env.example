# AI Debugging Configuration
# Add these to your main .env file to enable debugging

# Debug Prompts - Load from ai_debugging/prompts/agent_X.md files
PROMPT_DEBUG=true

# Debug Schema - Load from ai_debugging/schemas/debug_schema.json
SCHEMA_DEBUG=true

# Debug Tools - Load ALL tools from ai_debugging/tools/*.json (available to ANY agent)
TOOLS_DEBUG=true

# Usage:
# 1. Set any of these to true in your .env file
# 2. Edit files in ai_debugging/ directory
# 3. Changes take effect immediately (no restart needed)
# 4. If file doesn't exist, falls back to MongoDB automatically
